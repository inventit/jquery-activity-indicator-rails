RSpec.describe Jquery::Activity::Indicator::Rails do
  it "has a version number" do
    expect(Jquery::Activity::Indicator::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
